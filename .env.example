# Environment Variables for EDI Management System

# Server Configuration
NODE_ENV=production
PORT=3000

# Session Configuration (IMPORTANT: Change this to a random string)
SESSION_SECRET=change-this-to-a-random-32-character-string-in-production

# Authentication
ADMIN_PASSWORD=1234

# Optional: Database (we'll add this later)
# DATABASE_URL=postgres://username:password@host:5432/database

# Security Settings for Production
HTTPS_ONLY=false
TRUST_PROXY=true